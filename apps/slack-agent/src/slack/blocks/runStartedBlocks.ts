type KnownBlock=Record<string,any>;
import { safeSlackUrl,truncateSlackText } from './safety.js';
export function runStartedBlocks(i:{project:string;environment:string;intent:string;status:string;runId:string;webUrl?:string}):KnownBlock[]{const url=safeSlackUrl(i.webUrl);return[{type:'section',text:{type:'mrkdwn',text:truncateSlackText(`*Talos test started*\n*Project:* ${i.project}\n*Environment:* ${i.environment}\n*Intent:* ${i.intent}\n*Status:* ${i.status}`)}},{type:'actions',elements:[{type:'button',text:{type:'plain_text',text:'Stop run'},style:'danger',action_id:'talos_stop_run',value:i.runId},...(url?[{type:'button',text:{type:'plain_text',text:'Open in Talos'},url,action_id:'talos_open_run'}]:[])]}]}
