type KnownBlock = Record<string, any>;
export function runStartedBlocks(i:{project:string;environment:string;intent:string;status:string;runId:string;webUrl?:string}):KnownBlock[]{return[{type:'section',text:{type:'mrkdwn',text:`*Talos test started*\n*Project:* ${i.project}\n*Environment:* ${i.environment}\n*Intent:* ${i.intent}\n*Status:* ${i.status}`}},{type:'actions',elements:[{type:'button',text:{type:'plain_text',text:'Stop run'},style:'danger',action_id:'talos_stop_run',value:i.runId},...(i.webUrl?[{type:'button' as const,text:{type:'plain_text' as const,text:'Open in Talos'},url:i.webUrl,action_id:'talos_open_run'}]:[])]}]}
