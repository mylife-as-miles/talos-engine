type KnownBlock=Record<string,any>;
import { truncateSlackText } from './safety.js';
export function confirmationBlocks(input:{project:string;environment:string;baseUrl?:string;intent:string;id:string}):KnownBlock[]{return[{type:'section',text:{type:'mrkdwn',text:truncateSlackText(`*:warning: Confirm production-like Talos run*\n*Project:* ${input.project}\n*Environment:* ${input.environment}\n*Base URL:* ${input.baseUrl??'Not provided'}\n*Intent:* ${input.intent}\n\nThis can affect a production-like environment. Confirm only if you intend to run it.`)}},{type:'actions',elements:[{type:'button',style:'danger',text:{type:'plain_text',text:'Confirm run'},action_id:'talos_confirm_run',value:input.id},{type:'button',text:{type:'plain_text',text:'Cancel'},action_id:'talos_cancel_run',value:input.id}]}]}
